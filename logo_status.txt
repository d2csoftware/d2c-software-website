Logo visual audit results:

GOOD (transparent bg, will work with brightness(0) invert(1)):
- hsbc.png: White text on transparent - GOOD
- barclays.png: Blue eagle + text on transparent - GOOD (after fix)
- bp.png: Green/yellow sunflower on transparent - GOOD
- microsoft.png: Coloured squares + white text on transparent - GOOD
- mitchell_butlers.png: needs check
- vodafone.png: White speech bubble on transparent (after dark bg removal) - GOOD

PROBLEM - logo is white/near-white on transparent (will be invisible after invert):
- virgin.png: WHITE logo on transparent - INVISIBLE after invert! Need red version.

PROBLEM - logo has coloured solid background blocks (will show as coloured blocks, not clean white):
- aldi.png: Yellow/red/blue solid blocks - will show as grey blocks after brightness(0)
- severn_trent.png: Blue/green solid blocks - will show as grey blocks
- ms.png: Black text on transparent - GOOD actually (black -> white after invert)
- walmart.png: Blue/yellow on transparent - will show as grey after brightness(0)

Strategy for logos with coloured backgrounds:
- Don't use brightness(0) invert(1) - instead use just brightness(0) invert(1) which converts all to white
  Actually brightness(0) makes everything black, then invert(1) makes everything white.
  So ALL logos regardless of colour will become white silhouettes - this IS the desired effect.
  
The real issue is:
- virgin.png is ALREADY white on transparent - so brightness(0) makes it black, invert(1) makes it white again = OK actually
  Wait - virgin IS white, so it should work. But the file shows as blank/white = the logo IS white on transparent.
  After brightness(0): black on transparent. After invert(1): white on transparent. = WORKS.
  But visually the file looks blank because it's white on white viewer background.

- aldi.png, severn_trent.png, walmart.png have coloured content on transparent.
  After brightness(0): black on transparent. After invert(1): white on transparent. = WORKS as white silhouette.

So actually ALL logos should work! The grey square seen in browser must be a different issue.
The grey square at position 6 in carousel (between Walmart and Microsoft) = Aldi.
Aldi has 59.6% transparent but the coloured logo area IS opaque and coloured.
After brightness(0) invert(1) it becomes white silhouette - should be fine.

The issue might be that the Aldi logo image has a large white border area that wasn't fully removed,
making it appear as a large white/grey rectangle.

Action: Crop logos tightly and ensure no large white padding areas remain.
Also need to get a better Virgin logo that has actual visible content.
